apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: django-app
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}